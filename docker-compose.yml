version: '3'
services:
  view:
    build: ./view
    container_name: "kure-portfolio"
    volumes:
      - ./view/portfolio/portfolio:/app
    command: sh -c "npm install && npm run dev"
    ports:
      - "3000:3000"
    stdin_open: true
    tty: true